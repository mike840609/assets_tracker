.env
.env.*
!.env.example

# Tests and reports — not needed at runtime
tests/
playwright-report/
test-results/
playwright.config.ts

# Documentation — not needed at runtime
docs/
*.md
!README.md

# README media — rendered by GitHub from the repo, never served by the app
public/readme-*

# Local AI / agent tooling — never needed at runtime
.agent/
.agents/
.claude/
.codex/
.impeccable/
.worktreeinclude
Dockerfile
.dockerignore
docker-compose.yml

# CI / git tooling — not needed in Vercel build
.github/
.husky/
