- 2026-05-17: [MOD]: Vercel Fluid CPU optimization P1+P2 — shrink billed Active CPU by skipping bot/scanner probes upstream of any function. Evidence-grounded plan in `docs/vercel_fluid_cpu_optimization_20260517.md` (supersedes the earlier `codex_*` draft): Vercel MCP runtime logs for the last 7 days showed 33 edge-middleware invocations and **zero** serverless invocations, with `/wp-admin/install.php`, `/cmd_sco`, and rapid 5×/sec `/privacy` scrapes dominating the traffic — bot-driven middleware fan-out is the actual hot path, not the cron or refresh routes the earlier draft focused on. **P1 (proxy.ts matcher):** widened the negative-lookahead in `src/proxy.ts` to skip `wp-admin|wp-login|wp-content|wp-includes|wordpress|xmlrpc|cgi-bin|cmd_|phpmyadmin|adminer|vendor/phpunit` plus any path containing `.php|.aspx?|.jsp|.cgi|.env|.git|.svn|.htaccess|.htpasswd|robots.txt|sitemap.xml`; bot tokens are start-anchored to avoid skipping legitimate routes, dotfile/extension tokens use `.*` so they match anywhere. **P2 (Vercel Firewall):** rules committed to `docs/firewall_rules.json` (5 rules: wp probes / dotfile + source probes / php-ext probes / scanner paths / 30-req-per-min rate-limit on `/login` `/privacy` `/terms`) with apply instructions in `docs/firewall_setup.md` (dashboard + REST API paths). Companion `public/robots.txt` added so cooperative crawlers honor the same boundary. Note: CLAUDE.md still refers to `src/middleware.ts` — Next.js 16 renamed middleware → proxy and the file is actually `src/proxy.ts`; the docs entry remains TODO. Verification plan in the optimization doc — re-pull MCP logs and the Vercel Usage dashboard after the firewall is applied and compare 24-h Active-CPU delta.
- 2026-05-17: [ADD]: UI/UX Sweep — Touch Targets, Chart Clarity, Polish (18 items across Bundles A/B/C). Fresh code-grounded audit produced concrete fixes catalogued in `docs/UI_UX.md` under the new 2026-05-17 addendum. **Bundle A (touch targets, 6):** AccountsSummary sort buttons + Analysis/Trend range chips + account-detail sort pills bumped to ~40 px on mobile (`accounts-summary.tsx`, `analysis-view.tsx`, `trend-chart.tsx`, `account-detail.tsx`); account-detail sort row gains mobile-only `sticky top-0 z-10 bg-background/90 backdrop-blur-sm border-b -mx-6 px-6 py-2 md:static …` (closes UI_UX #6); `MobileHeader` controls drop `scale-90` and privacy promoted to `h-10 w-10`; accounts-list bulk-select checkbox switches to `opacity-100 md:opacity-0 md:group-hover:opacity-100` with `p-2 -m-2` hit-zone and corresponding `pl-6 md:pl-0 md:group-hover:pl-6` content shift; `MobileHeader` gains a `ChevronLeft` back button on nested routes (gated by `LargeTitleContext.isVisible === false`, `router.back()` + `hapticTick()`); `common.back` added to both locales and `app/layout.tsx:264` `pickMessages` extended with `common` so `MobileHeader` can read it. **Bundle B (chart clarity, 5):** CashFlowChart gains a static two-dot legend above the bars (`var(--chart-2)` / `var(--chart-1)`); CategoryTrendChart `<Legend>` switched to `iconSize={8}` + `wrapperStyle={{ paddingTop, lineHeight: "18px", width: "100%" }}` to wrap on 320 px; TopMoversList changes render in colored pills (`bg-[color-mix(in_oklch,var(--chart-1)_18%,transparent)]` / `bg-destructive/15`) with explicit `+`/`−` prefix (color-vision safety); ProjectionChart YAxis gains rotated `(${baseCurrency})` inside-left label (axis width 52 → 64); history table `—` change cell wrapped in `<span title aria-label cursor-help>` reading new `history.noPreviousSnapshot` key. **Bundle C (polish, 7):** NetWorthCard delta split into pill + adjacent percent text with `flex-wrap` (no more horizontal scroll hiding the percent); account-name `<h2>` gains a `Pencil` icon affordance + `role="button" tabIndex={0}` + Enter/Space handler + new `accountDetail.editName` i18n key; mobile "Show N more" becomes `<Button variant="ghost" size="sm">`; `KpiTiles` now reads `useDensity()` (compact uses `text-xl` + `p-3`); density-toggle active state adds `border border-border` + `font-semibold` and `aria-pressed`; login trust-badge icons `w-3.5` → `w-4` (3 sites); ThemeToggle pre-hydration skeleton now mirrors the active control structurally (`p-0.5` outer + three `p-1.5` slots with `h-4 w-4 bg-muted` inner), eliminating slot-reshape on hydration. Cross-refs in `docs/UI_UX.md`: #6 ⚠️→✅, M3 ⚠️→✅, #4 partially advanced via B3 + C2. `npm run format:check / lint / typecheck` green after each bundle.
- 2026-05-16: [MOD]: R5 / S3 — ownership-check audit on `/api/accounts/[id]/...` mutation routes. `src/app/api/accounts/[id]/transactions/[transactionId]/route.ts` PATCH/DELETE were raw `export async function` declarations that only validated `tx.accountId === accountId` from the URL — any authenticated user could mutate another user's transactions given the IDs. Both handlers are now wrapped in `withAuth` with an upfront `Account.findUnique({ id: accountId, userId })` guard; `invalidateAccountCaches` switched from an extra DB round-trip to taking `userId` directly. `src/app/api/accounts/[id]/holdings/route.ts` POST never verified the URL account belonged to `userId`, and PATCH/DELETE called `prisma.holding.update/delete({ where: { id } })` with no user scoping at all (the `withAuth` wrapper only proved the caller was logged in). POST now gates on the parent account; PATCH/DELETE resolve the holding via `prisma.holding.findFirst({ where: { id, account: { userId } } })` before touching it. The other `/api/accounts/[id]/...` routes (`route.ts`, `cash-transactions/route.ts`, `transactions/route.ts` GET) were already correctly scoped. `INFRASTRUCTURE.md` R5 flipped ⚠️ Partial → ✅; `SUGGESTIONS.md` items 136-138 flipped ❌ → ✅; `suggestions_20260515.md` S3 flipped ⚠️ → ✅.
- 2026-05-16: [ADD]: Animation polish — A1/A2/A3 in `docs/UI_UX.md`. (A1) Theme toggle now uses the View Transitions API to radial-reveal the new theme from the click origin: `theme-toggle.tsx` writes click coords to `--vt-theme-x` / `--vt-theme-y` and runs `document.startViewTransition(() => flushSync(setTheme))`; `globals.css` adds `@keyframes vt-theme-radial-reveal` (`clip-path: circle(0 → 150vmax)`) gated by `data-vt-theme` attribute; falls back to instant swap on Firefox / Safari < 18 and under `prefers-reduced-motion`. (A2) Chart range chips on `/analysis` and dashboard trend chart now crossfade via `motion.div` keyed by `range` (180–220 ms opacity + 4 px lift). (A3) Mobile holdings rows (`account-detail.tsx`) and account cards (`accounts-list.tsx`, `CategorySection`) FLIP-animate via `motion.div` `layout="position"` + `AnimatePresence initial={false}`; springs from `src/lib/motion.ts`; desktop table rows intentionally excluded. All three gated by `useReducedMotion()`.
- 2026-05-16: [MOD]: Build / CI / E2E speed pass. **CI** (`.github/workflows/ci.yml`): split sequential install→format→lint→typecheck→build into a fan-out — one `install` job seeds a `node_modules + src/generated/prisma + ~/.cache/prisma` cache (keyed by `package-lock.json` + `prisma/schema.prisma`) that the other four jobs restore in parallel. `build` additionally restores `.next/cache` across runs. Added `concurrency: cancel-in-progress` per ref and `paths-ignore: docs/**, **.md` to short-circuit doc-only PRs. **E2E** (`.github/workflows/e2e.yml`, `playwright.config.ts`, `tests/e2e/smoke.spec.ts`): cache `~/.cache/ms-playwright` keyed by the `@playwright/test` version in the lockfile (warm runs only `playwright install-deps chromium` instead of redownloading); share the CI `node_modules` cache. Playwright config flipped to `fullyParallel: true, workers: 2`; the `waitForPageReady` `networkidle` helper was removed in favor of explicit `expect(...).toBeVisible()` waits. Concurrency keyed by deployment SHA. **Vercel**: `vercel.json` gains `installCommand: "npm ci --prefer-offline --no-audit --no-fund"`; `.vercelignore` now excludes `tests/`, `playwright-report/`, `test-results/`, `playwright.config.ts`, `docs/`, `*.md` (except `README.md`), `.github/`, `.husky/`, and `scripts/compress-og-images.mjs`; `scripts/vercel-build.mjs` now skips `prisma migrate deploy` when no files under `prisma/migrations/` changed since `VERCEL_GIT_PREVIOUS_SHA` (falls open on any uncertainty — missing SHA, shallow clone, or git failure; `FORCE_PRISMA_MIGRATE_DEPLOY=1` overrides; existing `SKIP_PRISMA_MIGRATE_DEPLOY=1` short-circuit preserved). Cross-refs: V15 → ⚠️ Partial, R20 / R21 addenda in `INFRASTRUCTURE.md`.
- 2026-05-16 (docs): [MOD]: Updated CLAUDE.md, README.md, and docs/ to reflect shipped features. CLAUDE.md gains: `projections/` and `api/options/chain/` routes, `account-service.ts`/`projection-service.ts`, `history/`/`projections/`/`analysis/` component dirs, new lib utilities (`env.ts`, `logger.ts`, `enums.ts`, `chart-formatters.tsx`, `i18n-utils.ts`), optional env vars, and OPTION holding-type note. README.md: Node.js updated to 24.x, features list expanded (FIRE projection, options, command palette, mobile native feel, color schemes), stale `ANALYSIS_ROADMAP.md` roadmap link replaced with `suggestions_20260516_future_features.md`. SUGGESTIONS.md: items 30/55 (structured logging), 122 (undo delete), 134/135 (Prettier + Husky) marked ✅ Done. future_features.md: F2 marked ✅ Shipped.
- 2026-05-16: [MOD]: Fix Prisma slow-query warnings in local dev. Raised slow-query threshold from 100 ms to 1000 ms in `prisma.ts` (WebSocket-to-Neon latency made 100 ms impractical). `getExchangeRate()` switched from two `findFirst` calls on `(fromCurrency, toCurrency)` to `findUnique` on the `id` primary key (`"${from}_${to}"`). `getMonthlyCashFlow()` refactored to eliminate a JOIN-based relation filter (`account: { userId }`) and per-row `include: { account }` — now fetches account IDs + currencies up-front (parallel with `getAllExchangeRates()`), queries `cashTransaction` directly with `accountId: { in: [...] }` (uses `@@index([accountId, createdAt(sort: Desc)])`), and resolves currency from an in-memory Map. Added `"use cache"` directive to `getMonthlyCashFlow` (tags: `accounts:${userId}`, `history:${userId}`; life: hours) so the analysis page no longer re-runs Prisma queries on every load.
- 2026-05-15: [ADD]: Database audit and performance fixes (DB1–DB14). Added `docs/DATABASE.md` as the new database-specific system of record (schema overview, enum inventory, DB1–DB14 backlog). Executed on all Neon branches: VACUUM ANALYZE on bloated tables (DB1), enabled `pg_stat_statements` v1.10 (DB2). Extended `cacheLife` from `"minutes"` to `"hours"` in `exchange-rate-service.ts`, `net-worth-service.ts`, and `history-service.ts` — eliminating ~95% of unnecessary seq scans on ExchangeRate/Account/Setting (DB11). Dropped 5 zero-use indexes via migration `20260515000000_drop_unused_indexes`: `Account_userId_idx`, `Holding_symbol_idx`, `Holding_assetType_expiration_idx`, `NetWorthSnapshot_userId_idx` (DB5, DB12). Updated `CLAUDE.md` to reference `docs/DATABASE.md`.
- 2026-05-15: [MOD]: Desktop accounts table extended with Currency, Native value, and Allocation % columns. `accounts-list.tsx` desktop branch gains three new columns at `lg+`: Currency (ISO code, monospace), Native value (em-dash when same as base), and Allocation % with a 56px inline progress bar (mirrors holdings-table weight bar). Allocation denominator is scoped per section so asset and liability bars each stay 0–100%. `colSpan` updated to 9. Three new i18n keys (`colCurrency`, `colNative`, `colAllocation`) added to both `en-US.json` and `zh-TW.json`. Mobile view unchanged. Extends D4 in `docs/UI_UX.md`.
- 2026-05-15: [ADD]: `docs/suggestions_20260515_vercel_mcp.md` — Vercel MCP-derived findings F1–F8 (companion to `suggestions_20260515.md`). Captures the 2026-05-14 platform snapshot (project flag `live: false`, empty 7-day runtime logs, build warnings) and re-prioritizes S4 / S5 / S6 / S8 / S20 in the S-doc. Also pins `package.json` `engines.node` from `">=22"` to `"24.x"` (closes F3, silences the recurring build warning). Adds a 2026-05-14 audit-pass note + `live: false` reference to `INFRASTRUCTURE.md` § Vercel Platform.
- 2026-05-15: [ADD]: `docs/suggestions_20260515.md` — 32-item prioritized enhancement digest (S1–S32 across four tiers: Crucial / High-Value / Polish & DX / Future), cross-referencing existing tracker IDs (R-series, V-series, Q-series, C-series, UI_UX M-series and Phases 3/4). Single "what to do next" roadmap on top of the lens-split trackers in `docs/`. No code changes.
- 2026-05-15: [MOD]: Mobile accounts page visual refresh + dashboard enhancements. (1) Accounts list: added 3-column mobile summary strip (Assets / Net Worth / Liabilities) above category sections, computed from existing `accountBaseValues` memo; respects privacy mode. (2) Account cards (`AccountCardWithHoldings`): simplified value display — removed heavy `bg-foreground` currency badge, replaced with muted uppercase currency text; dropped quantity column from holdings sub-rows (symbol + name + value only); added secondary subtitle line showing holdings count and cash balance when applicable. (3) Dashboard accounts summary (`AccountsSummary`): added proportional background fill per row — `bg-primary/5` for assets, `bg-destructive/5` for liabilities — that extends to each account's % share of its group; hidden in privacy mode; animates on sort with `transition-[width] duration-500`. (4) Net worth trend chart (`TrendChart`): period change (Δ amount + Δ%) now displayed as a colored pill badge below the chart title, updating when range selector changes; green tint for gains, red tint for losses; hidden in privacy mode.
- 2026-05-14: [MOD]: Equal-height dashboard chart cards + trend chart fills card. Dashboard charts grid changed from `items-start` to `items-stretch` so the trend, allocation, and currency exposure cards share the same height at the xl (3-column) breakpoint. Trend chart card made `h-full flex flex-col`; chart container uses `flex-1` with a `useContainerSize` ResizeObserver hook so the Recharts `AreaChart` fills the available height dynamically. Bottom padding zeroed on `Card` (`pb-0`) and `CardContent` (`pb-0`) to eliminate empty space below the chart. New `useContainerSize` export added to `src/hooks/use-container-size.ts`.
- 2026-05-13: [ADD]: Desktop wide-screen UX — D4 accounts list data table. `accounts-list.tsx` now renders a `hidden lg:block` sortable `<table>` alongside the existing `lg:hidden` mobile card view. Columns: Name ↑↓ | Category (icon+label badge) | Holdings | Value ↑↓ | →. Assets and Liabilities appear as tinted section header rows inside `<tbody>`. Row click navigates to account detail; checkboxes stop propagation for independent selection. Sort defaults to Value desc; column headers toggle asc/desc. Native currency shown as secondary line when different from base currency. Added `colName`, `colCategory`, `colHoldings`, `colValue` i18n keys to both locales. Also added `docs/UI_UX.md` Desktop Wide-Screen Enhancements section (D1–D8) documenting all 8 identified desktop UX improvements.
- 2026-05-13: [ADD]: Pro broker desktop layout for accounts/holdings. `/accounts/[id]` redesigned with a two-pane desktop layout: sticky `AccountsNavPanel` (left, `w-44 xl:w-52`, `sticky top-6 self-start`) lists all accounts grouped by Assets/Liabilities for quick switching without back-navigation; `HoldingsTable` (right, new `<table>`) provides sticky sortable column headers (Symbol, Name, Type, Qty, Price, Mkt Value, Weight), per-row portfolio-weight progress bars, and hover-reveal ⋯ actions menu. Mobile retains existing swipeable card rows unchanged. Global layout container widened from `max-w-6xl` to `max-w-7xl`. New files: `accounts-nav-panel.tsx`, `holdings-table.tsx`. Modified: `account-detail.tsx` (conditional mobile/desktop rendering), `accounts/[id]/page.tsx` (parallel fetch of all accounts for nav panel), `(main)/layout.tsx` (max-w increase).
- 2026-05-13: [MOD]: Vercel config hardening (V34–V36). Extended `maxDuration` in `vercel.json` to `/api/prices/refresh` (60 s) and `/api/exchange-rates/refresh` (30 s) to prevent silent Hobby-plan 10 s timeouts on heavy price-fetch routes. Added `poweredByHeader: false` and `images.minimumCacheTTL: 86400` to `next.config.ts` (profile-picture cache extended from 60 s to 1 day). Removed a duplicate security-headers block from `vercel.json` that conflicted with the more complete `next.config.ts` headers (which includes HSTS).
- 2026-05-13: [MOD]: Mobile layout spacing + trend-chart crosshair enhancements. (1) Reduced `space-y-8` to `space-y-4 md:space-y-8` on all four main pages and the dashboard skeleton, reclaiming ~64 px of vertical space per page on mobile. (2) Tightened dashboard charts grid gap from `gap-6` to `gap-3 sm:gap-6`. (3) Reduced empty-state padding from `py-24` to `py-12 md:py-24`. (4) Upgraded `TrendChart` crosshair to a data-point-snapped crosshair using Recharts v3 hooks (`useActiveTooltipCoordinate`, `useYAxisScale`, `useActiveTooltipDataPoints`, `usePlotArea`) rendered via `<Customized>` — both lines now intersect at the exact chart point instead of following raw mouse y. (5) Reduced chart whitespace: `XAxis height={20}`, `YAxis width={42}`, `margin.bottom=0`, `CardContent pb-1 sm:pb-4`.
- 2026-05-12: [MOD]: Resolve all remaining bundle optimization items (B3–B5, B9, B12, B14). Removed unused `date-fns` package (zero import sites). Added `@base-ui/react` to `optimizePackageImports`. Set `preload: false` on GeistMono font (not critical-path). Marked B4/B5/B9 done: lucide-react tree-shaking confirmed via named imports + `optimizePackageImports`; recharts already split via `ssr:false` dynamic imports; next-intl already uses `pickMessages()` to send only 3 of 28 namespaces to the client.
- 2026-05-12: [MOD]: Fix dashboard action bar mobile layout — price-updated and snapshot tags now stay on a single row. Added `pricesUpdatedMobile` / `snapshotMobile` i18n keys (both locales) that show only the relative-time value without the label prefix on mobile (`sm:hidden` / `hidden sm:inline` swap), eliminating overflow risk from long English strings like "Prices updated 15 minutes ago". Container changed from `flex-wrap` to `flex-nowrap overflow-hidden min-w-0`; tag padding reduced to `px-1.5 py-0.5` on mobile (restores `px-2.5 py-1` at `sm+`); `shrink-0` and `whitespace-nowrap` added per tag. Changed container from `flex-wrap` to `flex-nowrap overflow-hidden min-w-0`; reduced tag padding from `px-2.5 py-1` to `px-1.5 py-0.5` on mobile (restores original on `sm+`); added `shrink-0` and `whitespace-nowrap` per tag to prevent internal text breaking or tag compression.
- 2026-05-12: [MOD]: Fix Recharts `width(-1) height(-1)` console warnings on dashboard. Replaced `ResponsiveContainer` + `mounted` state guard in `AllocationChart`, `CurrencyExposureChart`, and `TrendChart` with a new `useContainerWidth` hook (`src/hooks/use-container-size.ts`) that measures the container via `useLayoutEffect` + `ResizeObserver`. Charts now render only after real pixel dimensions are known, eliminating the initial -1 render that triggered the warnings.
- 2026-05-11: [MOD]: Dashboard mobile — compact `DashboardActions` bar. The "Refresh prices" button is now `hidden md:flex` (desktop-only) since `DashboardPullRefresh` already triggers the same `/api/prices/refresh` + `/api/exchange-rates/refresh` calls on mobile, making the explicit button redundant. The two info pills ("Prices updated …", "Snapshot …") retain the same styling on both sizes. Frees ~50 px of vertical space above the Net Worth hero on mobile so more data is visible without scrolling.
- 2026-05-11: [ADD]: Codebase review suggestions 144-151 — middleware JSON 401 for unauth `/api/*`, explicit `/api/_metrics/vitals` boundary, `history:${userId}` invalidation on cash-tx mutations, `Content-Security-Policy` header, batched cron expired-options sweep, lock down `updateHoldingSchema` (no `assetType` mutation), surface `refreshAllPrices` `errors[]` in the dashboard toast, and replace remaining `: any` casts in API routes. Detail under the 2026-05-11 section in `docs/SUGGESTIONS.md`.
- 2026-05-11: [MOD]: Fix mobile tab-switch lag — five compounding sources eliminated: (1) MobileNav `onTouchStart` now calls `router.prefetch()` so RSC fetch starts ~150 ms before tap commits; (2) all five tab-page fade-in animations reduced from 500 ms to 200 ms; (3) dashboard chart/accounts stagger delays halved (`delay-150→75`, `delay-300→150`); (4) `usePersistedRange` `startTransition` wrapper removed so chart range buttons are available immediately on mount; (5) net-worth count-up durations halved (1200/1000 ms → 600/500 ms).
- 2026-05-10: [ADD]: Crucial codebase review suggestions. Added SUGGESTIONS.md items 136-143 covering transaction API tenant-isolation gaps, transaction query/cursor validation, user-scoped manual price/FX refreshes, safer merge-first data import, CI-backed API ownership tests, and an explicit `/api/_metrics/vitals` proxy/rate-limit policy.
- 2026-05-10: [MOD]: V16 — Account detail burst reduction. Added `src/lib/services/account-service.ts` with React `cache()` wrappers for `/accounts/[id]` account lookup and account-detail price-map reads, keeping the ownership-safe dashboard account cache as the structural source. Refactored the account detail page off direct Prisma reads. Disabled automatic prefetch for repeated account detail links in the accounts list and dashboard account summary, plus the mobile bottom nav, to prevent viewport-triggered 5–8× RSC request bursts.
- 2026-05-10: [ADD]: UI/UX item 2 + item 7 completion — Theme-color meta + iOS splash screens. Added `themeColor` to `viewport` export with light (`#f9fafb`) / dark (`#0d1f1e`) media-query variants so iOS Safari tints the status bar to match the app. Generated 34 iOS splash screen SVGs (17 device sizes × light/dark) via `scripts/generate-splash-screens.mjs` in `public/splash/`, covering iPhone SE through iPhone 16 Pro Max and all iPad sizes. Configured `appleWebApp.startupImage` in metadata with device-specific media queries. Updated `statusBarStyle` from `"default"` to `"black-translucent"` for edge-to-edge PWA appearance. `<html lang="en">` remains hardcoded to preserve Next.js Partial Prerendering (PPR) of the document shell. Completes UI/UX item 7 (Status bar / safe areas / chrome).
- 2026-05-06: [ADD]: UI/UX item 12 — Undo-first destructive actions. `src/lib/undo-delete.ts` introduces `showUndoDeleteToast()`: items are removed from local state immediately, a Sonner toast with an Undo action button appears for 5 s, and the actual DELETE API call fires only on auto-close or manual dismiss. Holding delete (`account-detail.tsx`): optimistic `optimisticHiddenIds` Set filters the holdings list; native `confirm()` replaced on account delete with the same pattern (button disabled while toast runs, navigates to `/accounts` on commit). Transaction delete (`transaction-history.tsx`): `pendingDeleteIds` Set hides rows; confirmation Dialog and `handleDeleteConfirm` removed; `handleDeleteTx` handles all delete triggers (swipe button, full-swipe, dropdown). Added `common.undo` i18n key (en-US: "Undo", zh-TW: "復原") and `transactionHistory.deleteSuccessSymbol` message.
- 2026-05-03: [ADD]: UI/UX item 10 — Disclosure transitions. Enable Next.js 16.2 View Transitions API (`viewTransition: true` in `next.config.ts`). Add iOS-style slide keyframes (`vt-slide-in-from-right`, `vt-slide-out-to-left`, `vt-slide-in-from-left`, `vt-slide-out-to-right`) to `globals.css` using `html:active-view-transition-type(nav-forward/nav-back)` selectors and the existing `--ease-out-expo` timing function at 300 ms. Outgoing page translates to −30 % (not −100 %) for a parallax reveal matching iOS UINavigationController. `transitionTypes={['nav-forward']}` added to account-card Links in `accounts-list.tsx` and `accounts-summary.tsx`; `transitionTypes={['nav-back']}` added to the breadcrumb back Link in `account-detail.tsx`. `prefers-reduced-motion` override removes translate and shortens to 150 ms cross-fade. Works in Chrome 125+ and Safari 18.2+; browsers without View Transition support fall back to instant navigation.
- 2026-04-24: [ADD]: R1 — Add baseline security headers (HSTS, XFO, XCTO, Referrer-Policy, Permissions-Policy) to `next.config.ts`
- 2026-04-24: [ADD]: R3 — Add sliding-window rate limiter (`src/lib/rate-limit.ts`); apply to `/api/search` (60/min), `/api/exchange-rates` (30/min), and `/api/auth/*` (20/min via proxy middleware)
- 2026-04-24: [ADD]: R14 — Add 5 s timeout + 2-retry exponential backoff (500 ms, 1.5 s) to Yahoo Finance and CoinGecko calls in `src/lib/services/price-service.ts`; per-symbol fallback isolation on Yahoo Finance batch failure
- 2026-04-24: [ADD]: R20 — Add `.github/workflows/ci.yml`; runs `npm ci` → `prisma generate` → `lint` → `tsc --noEmit` → `next build` on every PR and push to master/main
- 2026-04-24: [ADD]: R21 — Add Playwright smoke E2E suite (`playwright.config.ts`, `tests/e2e/global-setup.ts`, `tests/e2e/smoke.spec.ts`, `.github/workflows/e2e.yml`); covers unauthenticated redirect → login, account + holding creation, and dashboard net-worth card + trend chart; auth stubbed via preview-credentials provider
- 2026-04-25: [MOD]: R9 — Verify Google OAuth consent screen is published & verified; marked as done in docs
- 2026-04-25: [ADD]: R6 — Add `/terms` (Terms of Service) page
- 2026-04-26: [MOD]: R15 (partial) + V2 — Isolate preview database via env-scoped `DATABASE_URL`; add `build:vercel` npm script (`prisma migrate deploy && next build`) and point `vercel.json` `buildCommand` at it so each Vercel deploy applies pending Prisma migrations against whichever DB is wired in for that environment (prod vs. shared `preview` Neon branch); CI / local `npm run build` stays as plain `next build` so it does not require a database. Pre-existing Neon branches still need a one-time `npx prisma migrate resolve --applied 202604120001_add_hot_path_indexes` to baseline the empty `_prisma_migrations` history before the next Vercel deploy succeeds.
- 2026-04-26: [MOD]: UI polish — Add favicon-style app icon (green gradient rounded square, white trending-up chart) to desktop sidebar and mobile header; icon sits left of stacked title + subtitle. Sidebar: separator line added between header and nav, subtitle downsized to `text-xs`, bottom padding tightened. Mobile header: flex containment fixed (`min-w-0`) so subtitle no longer clips on narrow screens. Accounts list: dark-mode category card borders softened (`dark:border-*-800/40`) and background opacities raised (`dark:bg-*-950/60`) for improved readability.
- 2026-04-27: [MOD]: Fix options account display by adding contract multiplier logic in accounts-list.tsx
- 2026-04-28: [MOD]: Unified add-holding UX — Collapse the two-step search→confirm flow into a single form view for both `HoldingForm` (account detail) and `QuickAddHolding` (accounts list "Add Item"), matching the `OptionBuilder` pattern: ticker search collapses to a compact card on selection, quantity input is always visible, no step navigation required. Add option support to `QuickAddHolding` via Stock/Option mode tabs (option tab embeds `OptionBuilder` with `onConfigure` callback → routes to account selection; OPTION asset type maps to BROKERAGE). Redesign `OptionBuilder` as a single-form step: underlying text input with Load/Search buttons fetches the option chain on demand; expiration dropdown and strike dropdown (both driven by live chain data) appear inline once the chain loads; Call/Put tabs update the strike list without leaving the form. Fix mode-tab bug in `HoldingForm` where switching to "Option" was immediately overridden by `resetForm()` resetting mode to "stock".
- 2026-05-02: [ADD]: UI/UX item 9 — Swipe actions on list rows. Install `framer-motion`. `HoldingRow` (`src/components/accounts/holding-row.tsx`) and transaction rows in `TransactionHistory` (`src/components/accounts/transaction-history.tsx`) now support left-swipe revealing blue Edit / red Delete action buttons — classic iOS list pattern. Implementation: `motion.div` with `drag="x"`, `dragDirectionLock`, spring snap points (stiffness 300, damping 30); snap threshold is 40 % of the 144 px reveal (two 72 px buttons); haptic tick fires at threshold crossing via `hapticTick()`. Accounts list skipped — it uses checkbox multi-select batch delete and account rows are navigation targets, so swipe would conflict. Desktop (≥ sm) retains the three-dot `DropdownMenu` fallback. `package-lock.json` regenerated without `--legacy-peer-deps` to fix `npm ci` sync error in CI.
- 2026-05-02: [MOD]: Enhance swipe-action UX on holding and transaction rows. Six improvements: (1) **Icon scale pop** — button icons animate from 0.65× to 1.0× as the row reveals, giving a native "pushing out" feel. (2) **Full-swipe shortcut** — dragging past 224 px (REVEAL_WIDTH + 80) triggers delete immediately on release without needing to tap the button; holding rows delete directly, transaction rows open the confirm dialog to preserve the quantity-warning safety step. (3) **Edit shrinks in danger zone** — past 224 px the Edit button's width and opacity animate to zero so the Delete button fills the full reveal width. (4) **Delete icon grows in danger zone** — Trash2 icon scales from 1.0× to 1.3× as a visual warning. (5) **Red edge tint** — an oklch red gradient bleeds in from the right edge of the draggable row as the user enters the danger zone. (6) **One-row-at-a-time** — `src/lib/swipe-row-registry.ts` (module-level close-callback registry) ensures opening any row auto-closes all others; double haptic tick fires on danger-zone entry.
- 2026-05-02: [ADD]: UI/UX item 1 — iOS 11+ large-title navigation bar pattern. `LargeTitleHeading` component replaces the page-level `<h2>` on all 5 main pages, rendering `text-4xl` (≈36 px) on mobile. An `IntersectionObserver` (−64 px top `rootMargin`) drives `LargeTitleContext.isVisible`; `MobileHeader` reads the flag to fade the logo out and centre a small page title as the user scrolls — mirroring iOS UINavigationController collapse behaviour. `LargeTitleProvider` in `(main)/layout.tsx` resets visibility on every route change so detail pages without a heading always show the logo.
- 2026-05-02: [ADD]: UI/UX item 3 — Bottom-sheet dialogs for mobile. Account, holding, and edit-holding dialogs now use Vaul `Sheet` (`side="bottom"`) with rounded top corners, drag handle, and swipe-to-dismiss on mobile (< md breakpoint). Desktop retains the original centred `Dialog`. Affected: `account-form.tsx`, `holding-form.tsx`, `edit-holding-dialog.tsx`.
- 2026-05-02: [MOD]: Dashboard polish — skeleton shimmer + net-worth hero mesh. Dashboard skeleton placeholders replaced `animate-pulse` with a reusable `.skeleton-shimmer` utility (left-to-right gradient sweep animation). Net-worth hero now renders an animated `.hero-mesh-positive` / `.hero-mesh-negative` overlay tinted by the day-delta direction, layered behind card content.
- 2026-05-03: [MOD]: iOS-style rounded pill swipe action buttons. Edit (blue) and Delete (red) reveal buttons on `HoldingRow` and transaction rows changed from square to `rounded-full` pill shape to match native iOS list action style.
- 2026-05-04: [ADD]: Desktop command palette (⌘K / Ctrl+K) + keyboard shortcuts + sticky history headers. Global `desktop-command-palette.tsx` mounted in `(main)/layout.tsx`. Covers: navigation (1–5 route keys, Vim-style `g d/a/h` chords), privacy toggle (⌘/Ctrl+⇧P), price refresh (⌘/Ctrl+⇧R), and sign out. Price-refresh logic extracted to `dashboard-actions.tsx`. Sticky month-group headers added to `history-table.tsx`.
- 2026-05-04: [ADD]: Persisted icons-only collapsible desktop sidebar. Sidebar supports a `w-[72px]` icons-only mode toggled via a footer control. Preference persisted to localStorage key `asset-tracker:sidebar-collapsed`. Full-width label text hidden in collapsed state; Tooltip appears on icon hover.
- 2026-05-04: [ADD]: Comfortable / Compact density toggle. `DensityProvider` (localStorage key `asset-tracker:density`, default `"comfortable"`) wraps `(main)` layout. Settings → Preferences exposes a segmented control. Compact spacing applied across: net-worth card, accounts list, holding rows, history table, analysis KPI tiles, top-movers list, allocation-chart legend, and accounts-summary rows.
- 2026-05-04: [MOD]: Command palette polish — i18n, platform-aware shortcuts, sign out. All palette strings i18n'd under `commandPalette` namespace (both locales). Shortcut hints adapt to Mac vs Windows/Linux. Sign-out action added. Sidebar gains a clickable search-bar button ("Search… ⌘K" / search icon with tooltip in collapsed state) that dispatches a `command-palette:open` custom event. Hydration mismatch fixed by hardcoding ⌘K in the sidebar (platform detection runs only after dialog opens).
- 2026-05-05: [MOD]: Sidebar collapse shortcut + three new keyboard shortcuts. Sidebar toggle bound to Ctrl+\\ / ⌘\\ (conflict-free across all major browsers). `?` toggles the command palette (same as ⌘K/Ctrl+K, making shortcuts self-discoverable). `N` dispatches a `new-item` custom event that opens the Add Account or Add Holding dialog depending on the active page. `G→N` / `G→S` extend the Vim-style go-to sequence to reach the Analysis and Settings tabs. All new shortcuts surfaced in the command palette.
- 2026-05-05: [MOD]: Fix conflicting browser keyboard shortcuts. Privacy-mode shortcut changed from Ctrl+Shift+P (→ Firefox Private Window) to Ctrl+Shift+Y. Price-refresh shortcut changed from Ctrl+Shift+R (→ hard-reload in all major browsers) to Ctrl+Shift+U. Sidebar shortcut changed from Ctrl+B (→ Firefox Bookmarks sidebar) to Ctrl+\\ (backslash, conflict-free on Chrome/Edge/Firefox/Safari). Key listeners, display labels, tooltip hints, and `aria-keyshortcuts` attributes updated in `desktop-command-palette.tsx` and `sidebar.tsx`.
- 2026-05-05: [ADD]: UI/UX item 11 — Inline validation + numeric keyboard optimization on forms. Implemented `inputMode="decimal"` and `inputMode="numeric"` on amount, quantity, and contract fields to trigger numeric keypads on mobile. Added on-blur validation with inline helper text and formatted currency/quantity values (using `Intl.NumberFormat`) upon blur for `account-form.tsx`, `holding-form.tsx`, `quick-add-holding.tsx`, `inline-balance-editor.tsx`, and `option-builder.tsx` to reduce entry errors and prevent jumpy typing.
- 2026-05-06: [ADD]: .worktreeinclude file for Claude Code worktrees. Includes `.env*` files to ensure local environment context is preserved when creating worktrees.
- 2026-05-10: [ADD]: UI/UX item 7 — Unified motion token system. Added `--duration-*` and `--ease-*` variables to `globals.css` and corresponding `.motion-*` utility classes. Standardized framer-motion springs into `src/lib/motion.ts`. Updated layout, dashboard, and accounts components to use the new motion tokens instead of scattered duration/ease classes.
- 2026-05-10: [MOD]: Fix hydration mismatch in PrivacyModeProvider for streamed Server Components. Replaced internal Context usage with `useSyncExternalStore` inside `usePrivacyMode` to ensure the client correctly hydrates with the server snapshot before synchronously updating from `localStorage`.
- 2026-05-11: [ADD]: UI/UX item 9 and 15 — Mobile chart interaction model. Added `usePersistedRange` hook to persist chart range selections across navigations via `sessionStorage`. Created `useChartCrosshair` hook to provide haptic feedback during chart crosshair dragging and sticky tooltips on mobile. Replaced `useState` with `usePersistedRange` in `trend-chart.tsx` and `analysis-view.tsx`. Attached crosshair haptic handlers to all Area, Bar, and Line charts. Bound `onTouchStart` and `onTouchEnd` on `allocation-chart` pie segments to reliably trigger hover expansions for mobile users.
